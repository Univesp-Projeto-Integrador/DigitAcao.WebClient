import { store as user } from "@/contexts/User"
import { store as course } from "@/contexts/Course"
import { store as lesson } from "@/contexts/Lesson"

export default {
    user,
    course,
    lesson
}